#include <stdio.h>

int main()
{
    int n,x,sum;
    printf("Enter the size of array");
    scanf("%d",&n);
    int array[n];
 
   for (x = 0; x < n; x++)
   {
     scanf("%d", &array[x]);
     sum = sum + array[x];
   }
 
   printf("Sum = %d\n", sum);

    return 0;
}
